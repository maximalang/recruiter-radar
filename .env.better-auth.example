# Recruiter Radar Better Auth identity foundation.
# Keep disabled until the website identity bridge acceptance is green.
BETTER_AUTH_ENABLED=false
BETTER_AUTH_URL=https://recruiter-radar.ru
BETTER_AUTH_SECRET=

# Private operator MCP OAuth intentionally remains on its separate fail-dark
# authorization architecture until a patched stable OAuth provider is selected.
