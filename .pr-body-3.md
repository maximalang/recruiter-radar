## Summary
Add the multi-source lead aggregation infrastructure (MultiSourceLeadGenerator, LeadAggregator, LeadScoringService, source-config) and replace stub data with injectable adapters so digestItems are caller-supplied and enrichment is testable in isolation.

The stack will surface this through API routes in PR-4.

## Stack
- PR-1 chore/crawler-typecheck-fix -> main
- PR-2 chore/remove-stale-review-docs -> PR-1
- **PR-3 (this)** -> PR-2
- PR-4 feat/leads-api-routes -> PR-3

## Notes
- New `LeadScoringService.generateAndScoreLeads` requires `digestItems: HhDigestItem[]` from the caller; consumers must load digest items before invoking the service.
- `MultiSourceLeadGenerator` accepts injectable adapters (`fetchCareerPage`, `fetchBusinessSignals`, `fetchRegistryData`); defaults are no-ops.
- Adds new tests for the aggregator, generator, and scoring integration.

## Test plan
- [x] npm run web:check
- [x] cd apps/web && npm test (452/452)
