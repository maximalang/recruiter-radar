import type { MetadataRoute } from "next";

import { OPERATOR_REQUISITES } from "@/lib/operatorRequisites";
import { PUBLIC_PLANS } from "@/lib/pricingCatalog";

const SITE_URL = OPERATOR_REQUISITES.website;

/**
 * llms.txt — AEO/GEO-слой: машиночитаемая карта смысла сайта для LLM
 * и answer-движков. Только подтверждённые факты продукта; цены и реквизиты
 * тянутся из тех же модулей, что и лендинг, чтобы не расходиться.
 */
export function GET() {
  const pilot = PUBLIC_PLANS.find((p) => p.code === "pilot");
  const monthly = PUBLIC_PLANS.find((p) => p.code === "monthly");
  const quarterly = PUBLIC_PLANS.find((p) => p.code === "quarterly");

  const body = `# Recruiter Radar

> ${SITE_URL} — информационно-аналитический онлайн-сервис для рекрутинговых агентств. Радар находит компании с активным наймом, объясняет, почему эта компания актуальна именно сейчас («почему сейчас»), показывает подтверждающие факты и безопасный официальный путь первого контакта.

Продукт решает задачу поиска новых клиентов для рекрутинговых агентств на рынке России. Радар НЕ является ATS, CRM, парсером резюме, инструментом массовых рассылок или sourcing-инструментом кандидатов: сообщения компаниям отправляет само агентство, сервис только находит повод и собирает доказательства.

## Что получает агентство

- Приоритетный список компаний с активным наймом под специализацию и географию агентства; частота обновления зависит от подключённых и проверенных источников.
- По каждой компании: причина актуальности («почему сейчас»), уровень уверенности (confidence A–D), факты и источники.
- Официальные каналы связи: карьерная страница компании, публичные реквизиты, общий контакт — без личных данных сотрудников.
- Профиль поиска: специализация, география, ключевые слова, лимит выдачи.
- Доставка в веб-кабинет; дополнительно подключаются Telegram и email-уведомления.
- Обратная связь по каждой находке (${`Беру / Мимо / Позже / Уже написал`} и др.) влияет на будущие приоритеты.

## Как работает оценка компаний

Каждая возможность оценивается по модели FIUR: Fit (соответствие профилю агентства) + Intent (признаки активного найма) + Urgency (срочность повода) + Reachability (доступность официального контакта). Итог от 0 до 4. Уверенность (confidence) определяется независимостью источников: от A (несколько независимых слоёв доказательств) до D (контекст без прямых доказательств найма — в выдачу не попадает). Источники: hh.ru, «Работа России» (trudvsem.ru), карьерные страницы компаний, публичные данные ФНС для проверки организации.

## Тарифы (разовая оплата периода, без автопродления)

${pilot ? `- «${pilot.name}»: ${pilot.price} за ${pilot.cadence}.\n` : ""}${monthly ? `- «${monthly.name}»: ${monthly.price} за ${monthly.cadence}.\n` : ""}${quarterly ? `- «${quarterly.name}»: ${quarterly.price} за ${quarterly.cadence}.\n` : ""}
Оплата разовая, автоматическое продление отсутствует. Возврат и порядок оплаты: [Условия оплаты и возврата](${SITE_URL}/payment-and-refund).

## Ключевые страницы

- [Лендинг](${SITE_URL}): интерактивный пример выдачи без регистрации, сценарий работы, тарифы, FAQ.
- [Правовая информация](${SITE_URL}/legal): реквизиты оператора сервиса.
- [Условия использования](${SITE_URL}/terms): оферта.
- [Политика конфиденциальности](${SITE_URL}/privacy).
- [Согласие на обработку персональных данных](${SITE_URL}/personal-data-consent).
- [Карта сайта](${SITE_URL}/sitemap.xml).

## Оператор сервиса

${OPERATOR_REQUISITES.brandName}. ${OPERATOR_REQUISITES.fullName}, ${OPERATOR_REQUISITES.status}, ИНН ${OPERATOR_REQUISITES.inn}, город ${OPERATOR_REQUISITES.city}. Контакт: ${OPERATOR_REQUISITES.email}, ${OPERATOR_REQUISITES.phone}.
`;

  return new Response(body, {
    headers: { "Content-Type": "text/plain; charset=utf-8" },
  });
}
