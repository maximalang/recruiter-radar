# Copy review: PR #238 (codex/seo-aeo-infra, head 5e6fc2bb) — AEO/SEO-слой

Дата: 25.08.2026 · Автор: @rr-mkt-content · Проверено по фактическим файлам
ветки (`git show origin/codex/seo-aeo-infra:...`), не по описанию PR.

## Вердикт

**Copy-зона согласована, blocker'ов нет.** Один остаток cadence-класса найден —
не в SEO-файлах, а в унаследованном `pricingCatalog.ts`; закрывается моим уже
запушенным коммитом `7b0800a7` при слиянии цепочки.

## Проверка по файлам

- **llms.txt route**: «Радар находит компании с активным наймом… почему сейчас…
  официальный путь первого контакта» — формула точности совпадает с лендингом;
  «частота обновления зависит от подключённых и проверенных источников» — та же
  нейтральная формулировка, что прошла в FAQ; identity-якоря (НЕ ATS/CRM/парсер,
  FIUR, confidence A–D, разовая оплата без автопродления) — корректны.
  Скан «ежедневн/каждый день/утро/за сегодня/вчера» и запрещённых claims — пуст.
- **seo-jsonld.ts** (Organization + WebSite + FAQPage): description нейтральный
  («радар компаний с активным наймом»), FAQ тянет те же строки `landing-faq.ts`,
  что рендерит страница — расхождения между JSON-LD и видимым текстом нет.
  Скан cadence/ban — чисто.
- **layout.tsx**: meta description «Радар компаний с активным наймом для
  рекрутеров и агентств.» — старое «Ежедневный радар…» снято. canonical +
  metadataBase из operatorRequisites — ок.
- **robots.ts / sitemap.ts**: приватные зоны (/api, /dashboard, /admin,
  /settings, /onboarding, /checkout) закрыты; sitemap только публичные маршруты.

## Остаток (не блокер #238)

`apps/web/lib/pricingCatalog.ts:22` в ветке seo всё ещё содержит буллет
«ежедневный приоритет компаний…». Это мой фикс из `7b0800a7` (#234-линия):
«приоритет компаний с подтверждёнными hiring signals». При merge #234 конфликт
строки решается в пользу content-ветки; regression-guard
`landing-final-production-contract.test.ts` уже включает pricingCatalog в
cadenceFiles и не даст строке вернуться. @rr-mkt-seo — можно не править в #238,
но если удобнее закрыть в своей ветке — замена однострочная, гард не против.

## Границы гарда

`seo-infra-contract.test.ts` покрывает ban-фразы для llms.txt и структуру
JSON-LD; рекомендую дополнить одним ассертом `not.toContain("ежедневн")` по
llms.txt/jsonld при следующем касании — не блокер для merge.

Статус: проверено, copy-согласие на #238 дано; позиция в цепочке — последний
gate перед deploy — поддерживается.
