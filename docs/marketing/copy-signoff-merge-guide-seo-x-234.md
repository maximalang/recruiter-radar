# Copy sign-off: merge-guide-seo-x-234 (e0e6fa70) × #234 head e9cf06d6

Дата: 25.08.2026 · Автор: @rr-mkt-content · Проверено против фактических веток.

## Вердикт

**Рецепт @rr-mkt-seo согласован как есть.** Итоговый metadata-блок из гайда
проверен построчно — расхождений с copy-стандартами нет.

## Проверка построчно

- **description из #234** («…компании с растущим наймом… факты, источники и
  официальный путь контакта») — подтверждён на remote-факте: именно эта строка
  в `home-page-content.tsx:22` на `e9cf06d6`. Класс blocker'ов чист:
  «растущий найм» — состояние найма, не обещание свежести/частоты.
- **OG-description seo-ветки** («Радар компаний с активным наймом…») — та же
  формула «активный найм», которая прошла гард в hero-описании
  (`detection-scene.tsx`: «Приоритетный список компаний с активным наймом»).
  Единая терминология лендинга и OG соблюдена.
- **`alternates.canonical` + `openGraph.url = "/"`** — copy не затрагивают;
  SEO-обвязка остаётся за @rr-mkt-seo.
- **title** — без изменений, pinned-класс строки.

## Гард после сведения

Regression-guard `landing-final-production-contract.test.ts` уже покрывает
`app/home-page-content.tsx` (в списке cadenceFiles) — после сведения веток
запустить его вместе с `seo-infra-contract.test.ts`; оба должны пройти без
правок текста. Если конфликт решён по рецепту — правки copy НЕ требуются,
это чисто механическое сведение.

Порядок deploy: #233 → #223 → #234 → #235 → seo-ветка следом — copy-зона
согласована.
