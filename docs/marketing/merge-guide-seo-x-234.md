# Rebase/merge guide: codex/seo-aeo-infra × #234 (codex/landing-hero-concept2)

Дата: 25.08.2026 · Автор: @rr-mkt-seo · Проверено `git merge-tree --write-tree` локально.
Обновление 1: перепроверено на копирайт-head `7868353c` — конфликт тот же один, рецепт не меняется.
Обновление 2 (финал): перепроверено на verdict-head **`6781bad9`** — конфликт тот же единственный, рецепт в силе. Именно этот head допущен вердиктом @rr-critic.

## Факт

- `main × #234 (e9cf06d6)`: merge-tree exit 0 — чисто.
- `6781bad9 × codex/seo-aeo-infra (c3d2e3bf)`: exit 1 — ровно один конфликт
  в `apps/web/app/home-page-content.tsx`, блок `export const metadata`.

## Причина

Обе ветки меняли один и тот же блок metadata description:

| Ветка | description |
|---|---|
| base (main) | «Recruiter Radar помогает… находить компании с активным наймом…» |
| #234 | «Recruiter Radar находит для рекрутинговых агентств компании с растущим наймом и объясняет, почему писать им стоит именно сейчас: факты, источники и официальный путь контакта по каждой компании.» |
| seo-ветка | «Recruiter Radar помогает рекрутинговым агентствам находить компании с активным наймом и понятным поводом для первого контакта.» + добавила `alternates.canonical` и `openGraph` |

## Разрешение (рекомендация)

Взять текст описания из #234 (это их копирайт-пасс), сохранив SEO-обвязку
нашей ветки. Итоговый блок должен выглядеть так:

```ts
export const metadata: Metadata = {
  title: "Recruiter Radar — компании, которым стоит написать сегодня",
  description:
    "Recruiter Radar находит для рекрутинговых агентств компании с растущим наймом и объясняет, почему писать им стоит именно сейчас: факты, источники и официальный путь контакта по каждой компании.",
  alternates: { canonical: "/" },
  openGraph: {
    type: "website",
    siteName: "Recruiter Radar",
    locale: "ru_RU",
    url: "/",
    title: "Recruiter Radar — компании, которым стоит написать сегодня",
    description:
      "Радар компаний с активным наймом для рекрутинговых агентств: почему сейчас, доказательства и безопасный путь контакта.",
  },
};
```

Примечания:
- OG-description seo-ветки («Радар компаний с активным наймом…») уже без
  cadence-слов — оставляем как есть.
- Всё остальное (JSON-LD script, robots/sitemap/llms.txt, layout metadataBase)
  конфликтов не имеет и сводится автоматически.
- После сведения прогнать: `npm run web:check`, Jest
  `seo-infra-contract.test.ts` + landing-контракты, затем build.

Проверка формулировки #234 на blocker-класс «свежесть без даты»:
«компании с растущим наймом» — ок (состояние найма, не обещание свежести
относительно «сейчас»); гард-скан слов «ежедневн/каждый день/каждое утро/
за сегодня» — 0 попаданий в этой строке.
