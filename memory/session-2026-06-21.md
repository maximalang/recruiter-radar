---
name: session-2026-06-21
description: Session log 2026-06-21 — source map state, rabota-rossii registered non-digest, prod now has habr-career+superjob+hh signals, superjob FIXED (corrupt key, not 403), open items (HH low yield, promotion recheck, onboarding, TG channels)
metadata:
  type: project
---

# Session 2026-06-21

## What was done today (commits in order, earliest → latest)

1. `ed59c92` fix(hh-ingest): populate `orgs.domain` from employer `site_url` — closes the null-domain silent gate noted in [[project_leads_pipeline_gaps]].
2. `3b9cd48` test(lead-discovery): cover career-page domain fallback in `MultiSourceLeadGenerator`.
3. `64bbab0` fix(hh-adapter): surface IP/geo 403 as actionable `HhAccessForbiddenError` (no more silent empty result on block).
4. `99d5ffd` feat(scoring): broaden competitor exclusions to bare staffing/recruitment roots.
5. `dcdfed9` fix(delivery): repair `client_profiles` ICP schema drift + add delivery diagnostics.
6. `6881f4b` fix(delivery): `confidence_gate` key case; feat(scoring): freshness decay tiers, contact quality; chore: habr legal review + memory update.
7. `d43b9a7` fix(career-pages): fail loudly when a live crawl normalizes 0 records — closes the last unguarded silent-zero-leads source ([[project_normalization_guard_coverage]]).
8. `78b4160` docs(source-review): reject avito, rabota-ru, trudvsem, zarplata-ru after live checks (ToS / data-quality / no live signal).
9. `46b0d4c` test(rabota-rossii): add live-data confidence verifier + `npm run verify:rabota-rossii:confidence`.
10. `2d22d3b` feat(sources): register `rabota-rossii` as a **non-digest signal-pool source**; also fixed a pre-existing false positive in the source-readiness direct-fetch guard (its regex `/\bfetch\s*\(/` matched a code *comment* "An empty fetch (...)", not a real HTTP call — reworded to "An empty crawl"). Pushed fast-forward `46b0d4c..2d22d3b`.

Validation at end: `web:check` green (74 suites / 842 tests), `verify:smoke` green, `verify-source-readiness` `ok: true` (15 sources). DB-dependent verifiers skipped locally (no DB in sandbox — expected).

## Current source map (registry intent vs prod reality)

Registry has **15 sources**. Live prod `org_source_refs` signal counts queried 2026-06-21 via Railway (`railway run --service Postgres node <script>` using `DATABASE_PUBLIC_URL` — the web service env only exposes the internal `postgres.railway.internal` host, unreachable from a laptop; query through the Postgres service to get the public proxy URL `acela.proxy.rlwy.net:47200`):

Queried later the same day against the **`signals`** table (the real signal store; `org_source_refs` is only the org↔source mapping, not signals — earlier I queried the wrong table):

```
SIGNALS BY SOURCE (table: signals):
  habr-career — 94 signals, latest 2026-06-21T17:39Z
  superjob    — 40 signals, latest 2026-06-21T17:38Z  (was 35 → +5 from a direct ingest run, signalUpsertsCompleted:5)
  hh          —  6 signals, latest 2026-06-20T12:51Z  (stale — not producing since yesterday)
  TOTAL       — 140
```

- **Live / producing:** `habr-career` (94) and `superjob` (40).
- **SuperJob — FIXED this session.** Root cause was NOT a 403/geo block: `SUPERJOB_API_APP_ID` in Railway was corrupt (leading quote + truncated secret — see [[project_superjob_key_corrupt]]). After re-pasting the full key, a direct `live-public` run fetched (apiTotal 60), normalized 5/5, and upserted 5/5 into `signals`. End-to-end key→fetch→normalize→upsert proven.
- **Low yield:** `hh` (only 6, stale since 2026-06-20) — under investigation; adapter surfaces `HhAccessForbiddenError` on 403 (`64bbab0`).
- **Signal-pool, non-digest (gated):** `rabota-rossii` — registered today, `isPrimary: false`, `promotionStatus: 'blocked-from-digest-pending-confidence-tests'`. Freshness 28% within active-30d vs 60% threshold (as of 2026-06-21). Ingests to the pool but is held out of the digest until the confidence gate passes. Promotion path: when `npm run verify:rabota-rossii:confidence` passes, flip `isPrimary: true`.
- **Blocked / not producing:** provider-token sources that need live config.
- **Reviewed & rejected (not added):** avito, rabota-ru, trudvsem, zarplata-ru — see `78b4160` (ToS / data quality / no live signal).

## Open items for next session

1. **HH low yield** — hh has only 6 signals, stale since 2026-06-20, vs superjob 40 / habr 94. Investigate whether it's a partial 403 (some requests blocked, not all), over-aggressive filtering in the adapter, or a different cause. Direct HH API call from prod is the first diagnostic. If fixable without a proxy, fix and re-ingest. Adapter already raises `HhAccessForbiddenError` (`64bbab0`) so a hard 403 would be loud. This is the largest potential lead source.
2. ~~**SuperJob 403 fix**~~ — **DONE** this session. Was a corrupt key, not a 403. See source map above + [[project_superjob_key_corrupt]].
3. **rabota-rossii promotion** — recheck the freshness gate in ~2 weeks (target ~2026-07-05). Re-run `npm run verify:rabota-rossii:confidence`; if freshness ≥ 60%, flip `isPrimary: true` in `apps/web/lib/sources/source-registry.ts` and clear the productionBlocker in `packages/db/scripts/source-registry.mjs`.
4. **First real paying client onboarding flow** — exercise the full activation loop end-to-end (landing → pilot activation → client profile → Telegram connect → first daily digest → feedback buttons) with a real agency. Likely surfaces gaps in client-profile ICP capture and digest delivery.
5. **Consider: Telegram job channels as a source** — only if compliant. Must respect Telegram ToS and channel terms (public channels only, no scraping of private/closed groups, rate-limited, attributed). Treat as evidence-context first; promote to lead-originating only behind the same confidence gates. Open question: entity resolution from free-form channel posts is noisy — needs the same "N records but 0 normalized" guard as the live-scrape sources ([[project_normalization_guard_coverage]]).

## Notes / gotchas confirmed this session

- Pre-commit hook runs `web:check` (tsc + full jest). The `console.error` lines in test output (`db down`, `getLastRunSourceReport is not a function`, `not json{{{`) are **intentional failure-path tests**, not real failures — suite is 842/842 green.
- `git push --ff-only` is not a valid flag (that's a pull/merge flag); plain `git push` already refuses non-fast-forward.
