# n8n service for Recruiter Radar — runs scheduled workflows 24/7 on Railway.
# Pinned official image: no build step, reproducible, fast cold starts.
# Bump the tag deliberately; verify the n8n changelog before upgrading.
FROM n8nio/n8n:1.123.57
